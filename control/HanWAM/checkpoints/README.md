# HanWAM checkpoints

This clean repository keeps checkpoint paths in configuration but does not
commit model weight files. Place trained artifacts such as
`hanwam_mode1.pt` in this directory when running training, evaluation, or the
servicer locally.
